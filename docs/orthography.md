# Ainu Orthographic Variation — Catalogue (moved)

This catalogue is implementation-agnostic and now lives in the shared
cross-implementation repo, alongside the `options.schema.json` it maps to:

➡️ **https://github.com/mkpoli/ainconv-tests/blob/main/orthography.md**

(It was here in `ainconv-rs` only for historical reasons; it covers all of
ainconv / ainconv-py / ainconv-rs, so it belongs in `ainconv-tests`.)
